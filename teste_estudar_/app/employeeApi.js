'use strict';

let employees = require('./employeeDB.json');
module.exports = (() => {

    return {
        getEmployee: getEmployee
    };

    function getEmployee(req, res, next) {
        let id = req.query.mat;

    function calcularIdade(employee) {
        let informacoes = calcularIdade(employee);
        
            var idade = generate-employeeDB.generateDayOfBirth.year;

            var idade = (year - 2016);
            
            return idade;

    }
    }
    })();